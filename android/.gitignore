# Gradle build outputs
build/
.gradle/

# Android Studio IDE files
.idea/
*.iml
local.properties

# gomobile-generated AAR (built by `make android-aar`; not committed)
app/libs/*.aar

# Signing keystores — NEVER commit; release key lives outside the repo
*.jks
*.keystore
