<?xml version="1.0" encoding="utf-8"?>
<!--
    Permit cleartext HTTP to:
      - 127.0.0.1: the embedded Go backend (always-on, auth-free loopback).
      - 10.0.2.2:  the Android emulator's alias for the host machine, used in
                   Phase 5 thin-client (Remote) mode so the emulator can reach
                   a sandboxed Cove instance running on the development host.
                   Traffic to 10.0.2.2 arrives at the host from a non-loopback
                   source address, so the host's token-auth middleware applies.
    Using per-domain config rather than android:usesCleartextTraffic="true" on
    <application> limits the exception to these two addresses — all other hosts
    still require TLS.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
    </domain-config>
</network-security-config>
