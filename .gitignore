/.env
/cove
/cove.exe
/library.json
/.flatpak-builder/
/*.flatpak
/coverage-*.out
/CLAUDE.md

# Gradle build output for the Compose Desktop app is ignored by app/.gitignore;
# only the Kotlin incremental-compile cache is missed by it.
/app/.kotlin/

# Editor / AI-tooling scratch
/.idea/
/.agents/
/.codex/

# JVM crash artefacts. They land in whatever directory the app was launched
# from, and three of them reached master once already.
hs_err_pid*.log
replay_pid*.log
core.[0-9]*

# Local, reversible copy made during the Kotlin cutover. The retired sources
# remain in Git history; only the explanatory marker is part of new checkouts.
/legacy/go-backend/**
!/legacy/go-backend/README.md
/.claude/
